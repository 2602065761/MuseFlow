# MuseFlow 工作流编排

## 完整流程

```
[用户输入艺术主题]
        │
        ▼
┌─────────────────────────────┐
│ Agent ①: 研究采集 (并行)    │  20-40 万 Token
│ - 4-6 维度并行搜索          │
│ - 视觉参考浏览器采集        │
│ - 去重聚类                   │
└──────────┬──────────────────┘
           │ 结构化研究简报 (JSON)
           ▼
┌─────────────────────────────┐
│ Agent ②: 概念提炼 (长链)    │  50-80 万 Token
│ Step 1: 主题解构             │
│ Step 2: 风格谱系定位         │
│ Step 3: 范式案例匹配         │
│ Step 4: 创作方向生成         │
│ Step 5: 可行性自评           │
└──────────┬──────────────────┘
           │
      ┌────┴────┐
      │ overall │
      │  >= 6?  │
      └────┬────┘
      No   │   Yes
      ▼    │    ▼
  [回退补充] │ ┌─────────────────────────────┐
  搜索       │ │ Agent ③: 方案生成           │  30-50 万 Token
             │ │ - 创作方案书 (Markdown)     │
             │ │ - 参考板 (HTML)             │
             │ │ - 答辩 Q&A 预演            │
             │ └──────────┬──────────────────┘
             │            │
             │            ▼
             │    [output/ 目录]
             │    交付用户
             │
             └──────────────┘
```

## 执行方式

### 方式一：手動串行（当前可用）

1. 将 `agents/research-agent.md` 的内容作为 Agent ① 的 system prompt
2. 用户输入主题 → Agent ① 运行 → 产出 JSON
3. 将 JSON + `agents/concept-agent.md` 交给 Agent ② → 运行
4. 将 Agent ② 输出 + `agents/synthesis-agent.md` 交给 Agent ③ → 运行
5. 获得最终交付物

### 方式二：自动化编排（后续计划）

通过 OpenClaw 的 cron/config 功能实现定时编排，或通过 Skill 封装为一步触发的完整流程。

## Token 预算总览

| 环节 | Agent | Token |
|------|-------|-------|
| 信息采集 | 研究采集 Agent | 20-40 万 |
| 概念提炼 | 概念提炼 Agent | 50-80 万 |
| 方案生成 | 方案生成 Agent | 30-50 万 |
| **单次合计** | | **100-170 万** |

## 环境依赖

- CherryStudio (Agent 运行平台)
- OpenClaw (Agent 编排)
- MCP Browser (视觉参考采集)
- Exa Search (文献/案例搜索)
